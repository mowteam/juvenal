name: bug-bounty
backend: claude

vars:
  REPO: ""               # Repository name or clone URL (e.g. "curl/curl" or "https://github.com/curl/curl")
  BOUNTY_SCOPE: ""        # Bug bounty scope document: what is in-scope and out-of-scope

phases:
  - id: hunt-bugs
    type: analysis
    prompt: |
      You are performing a bug-bounty-style analysis of the open-source project: {{ REPO }}

      ## Bug bounty scope

      {{ BOUNTY_SCOPE }}

      ## Mission

      Find real, reproducible bugs in this codebase. The bar is a working proof of concept (PoC),
      not a theoretical concern. Every claim you advance must be something a maintainer could
      reproduce by following concrete steps.

      **Strongly prefer memory-safety bugs that produce a sanitizer crash report (AddressSanitizer,
      MemorySanitizer, UndefinedBehaviorSanitizer, ThreadSanitizer) or a hard crash (SIGSEGV,
      SIGABRT, assertion failure).** These are the highest-value findings because:
      - The crash report is mechanically verifiable evidence — no judgment call needed
      - They almost always fall within scope of standard VRP / bug-bounty programs
      - They have a low false-positive rate once reproduced
      - They are easy to write up and easy for maintainers to triage
      This is a preference, not a hard requirement. Logic, injection, auth, and crypto bugs are
      still valuable when concrete and exploitable; just bias attention toward memory-safety
      surfaces first when the project is written in a memory-unsafe language (C, C++, unsafe Rust,
      Cython, native extensions, etc.). For memory-safe languages, fall back to the broader list below.

      ### What counts

      - **Memory safety bugs (HIGHEST PRIORITY)**: buffer overflows, use-after-free, double-free,
        uninitialized reads, OOB reads/writes, type confusion, integer overflow leading to memory
        corruption. **Gold-standard evidence: an ASan/UBSan/MSan crash report or a SIGSEGV trace.**
      - Logic errors that violate documented invariants or produce incorrect results
      - Injection and input-validation failures (command injection, SQL injection, path traversal, XSS, SSRF)
      - Authentication and authorization bypasses
      - Cryptographic misuse (weak primitives, nonce reuse, timing side-channels, improper key handling)
      - Race conditions and TOCTOU bugs with observable impact
      - Denial-of-service via algorithmic complexity, resource exhaustion, or crash-inducing input
      - Information disclosure (secrets leaked in logs/errors, unintended data exposure)
      - Deserialization and type confusion bugs

      ### What does NOT count

      - Stylistic issues, missing documentation, code-quality complaints
      - Theoretical attack surfaces with no concrete path from attacker-controlled input to impact
      - Bugs in test code, examples, or vendored third-party dependencies (unless the scope document says otherwise)
      - Findings that are explicitly listed as out-of-scope in the bounty scope above

      ### Captain guidance

      You are the strategic coordinator. Your job is to build a mental model of the project's
      architecture and attack surface, then decompose it into bounded investigation targets that
      workers can execute independently.

      **Operate at scale.** This workflow is configured for an exhaustive, multi-hour run. You
      should expect to spawn many dozens to hundreds of targets across the analysis. Run time is
      not a concern — coverage and depth are. The engine enforces explicit floors on captain
      turns and terminal targets and will reject premature `complete` declarations until those
      floors are met.

      **Per-turn fanout (hard expectation).** While `UNCOVERED SURFACE` in your
      `mental_model_summary` is non-empty, every turn must enqueue at least 8 new targets.
      Spread them across:
      - Unexplored subsystems and entry points pulled from `UNCOVERED SURFACE`
      - Variant-analysis follow-ups for recently verified or rejected claims (see policy below)
      - Deeper drills into the highest-yield surfaces already in flight

      **Variant-analysis policy (apply every turn that has new evidence):**
      - **Verified claim** (real bug found): the bug itself is a leaf — do NOT respawn the bug.
        Treat it as proof the surface is fertile. Spawn targets in the surrounding subsystem:
        sibling functions, callers and callees, related modules, structurally identical patterns
        elsewhere ("if pattern X exists here, where else does pattern X exist?").
      - **Rejected claim** (PoC failed or guard found): the path was wrong, not necessarily the
        surface. Spawn targets for: alternate paths to the same sink, sibling code that may LACK
        the verifier-identified guard, or a different vulnerability class on the same surface.
      - **No-findings target**: do NOT re-investigate the same scope. Only spawn an adjacent
        fresh-angle target if you have a concrete new reason.
      - **Blocked target**: do NOT respawn until the blocker is addressed (different build path,
        static-only approach, alternative tooling).

      Prioritize (memory-safety-rich surfaces first when the codebase is in a memory-unsafe language):
      1. Parsers/decoders/deserializers of untrusted input written in C/C++/unsafe-Rust — these are
         the highest-yield targets for ASan-crashing memory bugs (binary protocols, file format
         parsers, image/audio/video codecs, compression, regex engines, ASN.1/DER parsers, etc.)
      2. Allocation and lifetime management in unsafe/native code (manual malloc/free, custom
         allocators, refcounting, ownership transfers across FFI boundaries) — fertile ground for
         use-after-free and double-free
      3. Code surrounding previous bug patches and related code areas with similar code patterns
         (variant analysis: a fixed bug pattern often recurs in nearby code)
      4. Other code that parses, deserializes, or decodes untrusted input (network, file, IPC, env)
      5. Boundaries where privilege level changes (auth checks, permission gates, sandboxing)
      6. State machines and concurrency primitives (locks, atomics, async callbacks) — race
         conditions detectable via ThreadSanitizer count as memory-safety findings
      7. Integration points with external systems (databases, filesystems, subprocesses, DNS, HTTP)

      Start with entry-point enumeration and module-level surveys of the most exposed subsystems,
      then narrow to function-level and data-flow targets as evidence accumulates.

      Adapt aggressively: if a subsystem yields repeated no-findings or rejections, deprioritize it
      and pivot to unexplored attack surface. If a verified claim opens a new seam, follow it.

      #### Coverage expectations

      Your `mental_model_summary` MUST track coverage explicitly using the `SUBSYSTEMS`,
      `ENTRY POINTS`, `UNCOVERED SURFACE`, and `NOTES` sections defined in the captain role
      prompt. Every turn, update those sections in light of the latest event delta and the
      variant-analysis policy.

      Before declaring `termination_state: "complete"`, you must have:
      1. Enumerated ALL externally reachable entry points (network handlers, CLI parsers,
         file format parsers, IPC handlers, environment variable consumers, plugin/hook interfaces)
         in `ENTRY POINTS` and reached `covered` or `dry-hole` for each.
      2. Reached `covered` or `dry-hole` for every entry in `SUBSYSTEMS`.
      3. Followed every verified finding to adjacent attack surface per the variant-analysis policy.
      4. Explored at least 3 different vulnerability classes from the "What counts" list above.
      5. `UNCOVERED SURFACE` is empty (`(none)`).

      If you run out of high-priority targets, pivot to medium-priority exploration of
      unexplored subsystems and add them to your spawn batch — do NOT terminate. The engine
      will reject premature `complete` declarations until the configured floors are met.

      ### Worker guidance — PoCs are mandatory

      Workers: you are investigators, not theorists. Your job is to find concrete bugs and prove
      they are real. Every claim you submit MUST include a proof of concept.

      **What a PoC means:**
      - A specific sequence of steps, inputs, or commands that triggers the bug
      - For memory bugs (PREFERRED): a crafted input or call sequence that produces an
        AddressSanitizer / UBSan / MSan crash report, a SIGSEGV/SIGABRT, or an assertion failure.
        Capture the full sanitizer report (including the stack trace and the "SUMMARY:" line) and
        paste it into the claim. ASan output is the gold-standard PoC because it is
        mechanically verifiable and rarely contested.
      - For logic bugs: a concrete test case showing incorrect output vs. expected output
      - For injection/validation bugs: an actual malicious input string and the code path it takes
      - For race conditions: a reproduction script or interleaving description that triggers the
        race; ThreadSanitizer (`-fsanitize=thread`) output is the strongest evidence
      - For crashes/DoS: a minimal input or request that causes the crash or resource exhaustion

      **Prefer producing a sanitizer crash report when memory unsafety is plausible.** Concretely:
      - When the project is C/C++/unsafe-Rust, attempt to build with `-fsanitize=address,undefined`
        (or the project's existing ASan target — many projects ship one in `BUILD`, `CMakeLists`,
        Makefile flags, or a Dockerfile). If the build system supports fuzzers or test harnesses
        with sanitizers enabled, use them — they are the fastest path to a crash.
      - When the project has an OSS-Fuzz integration, look for `fuzz/`, `tests/fuzz/`, or
        `oss-fuzz/` directories. Reusing an existing fuzz harness is often the lowest-effort way
        to trigger a real crash on a candidate input.
      - When you cannot build with sanitizers (no toolchain, complex deps), construct the input
        anyway and document what crash signature ASan would produce (e.g., "heap-buffer-overflow
        WRITE of size 4 in `parse_header()` at parser.c:142"). The verifier may complete the
        sanitizer run.
      - If the bug class is not memory unsafety (e.g., logic, injection, auth), still produce a
        concrete trigger — sanitizer crashes are the gold standard but are not the only acceptable
        evidence.

      **PoC requirements:**
      - Write the PoC as a script, test case, or step-by-step command sequence in your report
      - Record the PoC commands in `commands_run` (include the build commands you used to enable
        sanitizers, if any)
      - If you ran the PoC and it triggered the bug, say so and include the output. **For
        sanitizer crashes, paste the full report.**
      - If the environment prevents running the PoC (no build toolchain, missing dependencies),
        write the PoC anyway and note the blocker — the verifier will attempt to run it
      - A claim without a PoC is incomplete. If you cannot construct a PoC, your confidence should
        be `low` and you should explain in `reasoning` exactly why a PoC was not possible

      **Why PoCs matter:**
      Static code reading alone produces false positives. Guards, sanitizers, type constraints,
      and caller-side preconditions are easy to miss. A PoC that actually triggers the bug is the
      difference between a real finding and a wasted cycle. The verifier will attempt to reproduce
      your PoC independently — if it does not reproduce, the claim will be rejected.

      Do not submit theoretical "this could be a bug" claims. Either prove it or return `no_findings`.

      **Output**
      Output a **brief** description of the bug and the PoC into `output/<bug-name>.md` and `output/<bug-name>-poc`, respectively.
    analysis:
      # Models default per role: claude → opus 4.7 1M for captain/worker,
      # sonnet 4.6 for verifier/reporter; codex → CLI default.
      # Override with `captain_model`, `worker_model`, `verifier_model`, or a
      # per-spec `model:` field if needed.
      captain_backend: claude
      worker_backend: claude
      max_workers: 4
      max_verifiers: 8
      interaction_timeout: 3.0
      max_worker_retries: 10
      max_captain_repairs: 4
      allow_repo_tools: true
      # Engine-enforced floors before the captain may declare `complete`.
      # The captain explores hundreds of targets across many turns; these floors
      # prevent early termination after the first wave of verified findings.
      min_captain_turns: 50
      min_terminal_targets_before_complete: 200
      # Soft escape: after this many consecutive overridden `complete` declarations,
      # the engine accepts completion to avoid an infinite nudge loop on dry codebases.
      max_premature_completes: 5
      continue_nudge: |
        ## Continue nudge — bug-bounty floors not yet met

        The engine has REJECTED your `complete` declaration (override #{consecutive} of
        {max_premature_completes}). Status: {turns} captain turn(s) elapsed; {terminal} target(s)
        in terminal state. Required floors: >= {min_captain_turns} captain turns AND
        >= {min_terminal_targets} terminal targets. This is a deep, exhaustive bug-bounty run —
        run time is not a concern; coverage is.

        Required actions for THIS turn:

        1. Update `mental_model_summary` with structured coverage accounting (SUBSYSTEMS,
           ENTRY POINTS, UNCOVERED SURFACE, NOTES). UNCOVERED SURFACE must be empty before
           `complete` is allowed.
        2. Apply the variant-analysis policy:
           - Verified claim: spawn targets in the surrounding subsystem (siblings, callers,
             callees, related modules, structurally identical patterns elsewhere). Do NOT respawn
             the same bug.
           - Rejected claim: spawn targets for alternate paths to the same sink, sibling code
             that may LACK the verifier-identified guard, or a different vulnerability class on
             the same surface.
           - No-findings target: do NOT re-investigate; only spawn an adjacent fresh-angle target
             if you have a concrete new reason.
           - Blocked target: do NOT respawn until the blocker is addressed.
        3. Enqueue at least 10 new targets pivoting to UNCOVERED SURFACE or following the
           variant-analysis rules above. Bias toward memory-safety-rich surfaces (parsers,
           decoders, allocators in C/C++/unsafe-Rust) for ASan-crashing PoCs.
        4. Return `termination_state: "continue"`. Do not declare `complete` again until both
           floors are met AND `UNCOVERED SURFACE` is empty.
      verifiers:
        - name: poc
          backend: claude
          prompt: |
            ## Your specialized scope: PoC reproducer

            You are verifier 1 of 4. You judge ONE thing: does this claim come with
            evidence that the bug is **real in the production code** and triggered
            **through the production code path** — not paraphrased, not in a mirror,
            not assuming a prior primitive.

            Apply the `bug-report-reviewer` skill if available. The rules below
            mirror that skill's PoC-quality bar.

            ### What you accept

            - Real memory-safety bugs (OOB read/write, UAF, double-free of a
              demonstrated reachable allocation, type confusion, uninit read,
              integer-overflow-into-corruption) demonstrated by a sanitizer
              (ASan / UBSan / MSan / TSan) running the **actual production
              code** — i.e., the PoC links against and executes the project's
              own source files / libraries / objects, or sends crafted input to
              a built production binary. Exploitability uncertainty does not
              disqualify these; a real OOB is a real OOB even if the primitive
              is weak.
            - Logic bugs (auth bypass, MAC/signature bypass, replay, cache
              poisoning that exceeds the protocol's documented trust model,
              info disclosure) demonstrated by a working PoC that drives the
              real binary or runs as a test in the project's own harness and
              shows the bug's effect.
            - Reachable abort/assert/SIGSEGV on a stock production build under
              attacker-controlled input (DoS).

            ### What you reject (set `verdict: REJECT` and pick a `rejection_class`)

            - **`reharness-poc`** — PoC reimplements the buggy algorithm in a
              self-contained file and demonstrates the flaw in the
              *reimplementation*. Mirroring the production code into a parallel
              source file and finding a bug there proves nothing about the
              shipped binary. Reject and ask for a PoC that links against /
              executes the real source. (Carve-out: faithful host-side ports of
              firmware code that genuinely cannot be linked into a host binary
              are acceptable, but only when the report establishes that
              constraint and the port is byte-for-byte identical, not a
              paraphrase.)
            - **`hypothetical-bug`** — "If somebody were to call X with Y the
              code would corrupt." No demonstrated caller, no demonstrated
              input. The bug must be in code that runs, not code that *could*
              run.
            - **`hardening-request`** — "The allocator should detect
              double-free", "the parser should reject empty input", "this
              `assert` should be a runtime check". These are improvements, not
              vulnerabilities, unless paired with a demonstrated reachable
              defect that exploits the missing guard.
            - **`primitive-chain`** — Bug only matters given a prior unproven
              primitive ("once an attacker has heap corruption, this gadget
              helps them..."). The bounty pays for the primitive, not the
              gadget. Bug also rejected if it presupposes the attacker
              position it would otherwise yield.
            - **`correctness-not-security`** — Wrong field interpretation,
              dropped packets, off-by-one in framing, interop violation. No
              memory safety, no auth bypass, no info leak. File as a regular
              PR.
            - **`insufficient-evidence`** — No PoC at all, or only
              code-pointing. In `follow_up_action`, name what PoC would
              suffice: the project's test harness, a fuzzer build, the exact
              crash signature you'd accept. For memory bugs, suggest the
              sanitizer build invocation.
            - **`guard-found`** / **`precondition-not-met`** — PoC exists
              but the trace shows a guard or unmet precondition between input
              and sink. Name the specific blocker.
            - **`wrong-sink`** / **`state-model-misread`** — PoC reproduces
              but produces a different effect than claimed (crashes the
              requester not the server, hits a benign assert, prints wrong
              output). Explain the mismatch.

            ### Verifiable evidence (preferred order)

            1. Sanitizer crash on the production binary or on a test/harness
               linked into the project's build (ASan stack trace, UBSan
               diagnostic, MSan use-of-uninit, TSan race) — show source line,
               input, and build flags.
            2. Production-binary crash (SIGSEGV / SIGABRT / assert) reproducible
               with one command or one input file.
            3. Behavioral evidence inside the project's own test harness — a
               unit/integration test that asserts the bug's effect on real
               state (acceptable for logic bugs).
            4. Code-only argument — lowest tier; only when the bug is
               mechanically obvious AND impact is unambiguous AND the path is
               short. Lean toward requesting evidence instead.

            ### Pass criteria (ALL must hold)

            1. The PoC executes or extends the **production code**, not a
               mirror.
            2. When run or traced, it reaches the cited sink and produces
               the alleged effect — corruption, crash, bypass, info leak,
               poisoning, etc. Static "this could happen" is not enough.
            3. Guards, type constraints, log-level gates, build-flag
               conditionals, and caller-side preconditions between attacker
               input and sink are explicitly addressed (you have walked them).
            4. The bug is not a hardening request, hypothetical, primitive
               chain, or pure correctness issue.

            ### What's out of scope for THIS verifier (later verifiers handle these)

            - Whether the project / module is in the bounty scope (`scope`).
            - Whether the bug is novel / unfixed (`novelty`).

            ### How to act

            **Run the PoC when the environment permits.** For C/C++/unsafe-Rust
            projects, actively build with the project's existing sanitizer
            target or add `-fsanitize=address,undefined`. A sanitizer crash you
            reproduce against the project's own build is the strongest verdict.

            **Challenge every weak submission.** Provide concrete
            `follow_up_action` and `follow_up_strategy` that point at the
            specific evidence still missing. For `reharness-poc` rejections,
            tell the worker exactly which production source file the PoC
            should link against, which test harness to extend, or which built
            binary to feed crafted input to. For `hardening-request` and
            `hypothetical-bug` rejections, name what reachable caller or
            attacker-controlled input would have to be demonstrated to convert
            the primitive into a finding.

        - name: root-cause-dedup
          backend: claude
          prompt: |
            ## Your specialized scope: root-cause de-duplication

            You are verifier 2 of 4. The `poc` verifier already confirmed a working
            reproduction. You judge ONE thing: is this claim a different *symptom*
            of the same *root cause* as another already-verified claim in this run?

            **Be lenient. Pass on ambiguity.** This verifier exists to suppress
            obvious duplicates so the final report is easier to parse — not to
            argue fine distinctions about whether two related bugs share a fix
            point. When unsure, PASS and let the reporter handle the writeup.

            ### How to find other verified claims

            The captain state lives in a JSON file at the project root, named
            `.juvenal-state-<phase-id>-analysis.json` (for this phase, look for
            `.juvenal-state-hunt-bugs-analysis.json`; if absent, fall back to
            any `.juvenal-state-*-analysis.json` in the current working
            directory). Read it (use `jq` if available) and filter
            `claims[*]` where `status == "verified"`.

            Each claim record carries:
              - `primary_location` → `path`, `line`, `symbol`
              - `subcategory` → bug class
              - `assertion` → the bug statement
              - `retry_of_claim_id` and `related_claim_ids` → captain-assigned
                links

            Also check whether the claim under review appears in another
            verified claim's `related_claim_ids` or `retry_claim_ids` — that's
            a captain hint that they overlap.

            ### Reject only when ALL of the following hold

            1. **Same root cause.** Either:
               - Identical `primary_location.path` AND `line` (or within ~5
                 lines) AND `symbol`, OR
               - Same `subcategory` AND the assertions describe the same
                 underlying defect mechanism (not just the same vulnerability
                 *class* — "two unrelated OOBs" is not a duplicate; "two
                 crash sites of the same parser-layer guard miss" is), OR
               - The fix is the same one-line / one-function change at a
                 shared upstream symbol (e.g., a parser-layer bound check
                 whose absence is the root cause of N call-site crashes).
            2. **Same fix retires both.** A single patch at one location
               would make the current claim's PoC stop reproducing AND make
               the other verified claim's PoC stop reproducing.
            3. **Compared against an already-verified claim, not a pending or
               rejected one.** Only verified parent claims qualify; otherwise
               two parallel-verified siblings could mutually reject each other.

            Captain hints (`retry_of_claim_id` pointing at a verified claim,
            or being listed in another verified claim's `retry_claim_ids` /
            `related_claim_ids`) are strong signals — when they line up with
            #1 and #2, REJECT confidently.

            If #1 holds but #2 does not (i.e., related but the fixes are
            distinct), PASS — it's a related-but-distinct finding worth
            keeping.

            ### Reject criterion

            - `rejection_class: duplicate-root-cause`
            - `summary`: name the parent verified claim ID and the shared
              root cause in one sentence
              (e.g., "duplicate of `target-posix-spinel-parser-g1-claim-c1`
              — both crash at distinct call sites of the same `spinel.c:617`
              `>=` parser bug, single one-character fix retires both").
            - `follow_up_action`: tell the worker the parent claim ID and
              that any further discovered call sites of this same root cause
              should be added as variant notes on the parent rather than
              filed as new claims.

            ### What's out of scope for THIS verifier

            - Whether the PoC works (`poc` handled this).
            - Whether the bug is in the bounty scope (`scope` handles this).
            - Whether the bug is already known to maintainers / public
              (`novelty` handles this — that's an external duplicate check;
              this verifier is the *internal* duplicate check across the
              current run).

            ### How to act

            Spend a small budget — one or two reads of the state file, then
            decide. If you cannot read the state file or cannot find clear
            evidence of overlap, default to PASS. False rejects on this
            verifier discard real findings; false passes only produce
            cosmetically redundant report rows that the reporter can fold
            together later.

        - name: scope
          backend: claude
          prompt: |
            ## Your specialized scope: bounty-scope gate

            You are verifier 3 of 4. The `poc` and `root-cause-dedup` verifiers
            already confirmed a working reproduction that isn't an obvious
            duplicate of another verified claim. You judge ONE thing: is this
            bug within the bounty's declared scope and reachable from a real
            attack surface?

            The bounty scope document:

            ```text
            {{ BOUNTY_SCOPE }}
            ```

            Pass criteria (ALL must hold):
            1. The cited code is in a component, package, or path that the bounty
               scope document declares in-scope.
            2. The bug is triggerable by a realistic attacker. Real attack surface
               means: public-facing API, network handler, file-format parser
               consuming attacker-controlled files, IPC handler, environment
               variable read in production, plugin/hook interface, or another path
               from untrusted input to the sink.
            3. The bug is NOT in any category the scope document explicitly excludes
               (test code, build tooling, vendored third-party libraries, CI/devnet
               configuration, debug-only flags, etc., unless the scope overrides
               those defaults).

            Reject criteria — pick the most accurate `rejection_class`:
            - In a path/component the scope explicitly excludes: `scope-too-broad`.
              Quote the specific scope-document line in `follow_up_action`.
            - In test/example/vendored/build code with no scope override:
              `scope-too-broad`.
            - No realistic attack surface — the trigger requires capabilities the
              attacker does not have (root on the host, write access to config
              files, control of an internal-only service): `precondition-not-met`.
              In `follow_up_action`, explain what realistic attacker capability
              would be needed and whether such capability is plausible.
            - Component is in scope but the cited entry point is not externally
              reachable: `wrong-source`. Identify a possibly-reachable entry point
              if you can find one.

            OUT OF SCOPE for THIS verifier:
            - Whether the PoC actually triggers the bug (already verified).
            - Whether the bug is novel (the next verifier handles this).

            Read the scope document and the project README before judging. Confirm
            the cited code is in a module the scope counts as "the project" rather
            than adjacent or vendored code.

        - name: novelty
          backend: claude
          prompt: |
            ## Your specialized scope: novelty / not-already-reported

            You are verifier 4 of 4. The previous verifiers confirmed a working
            PoC, that it isn't an internal duplicate, and that it is in scope.
            You judge ONE thing: is this bug NOT already known to maintainers
            or the public?

            Repository: {{ REPO }}

            Pass criteria (ALL must hold):
            1. The bug is not fixed in the current HEAD. Search recent commits
               (`git log --all --since=...`) and the CHANGELOG for keywords from
               the claim. If a commit fixes this exact code path or this exact
               bug class at this location, reject.
            2. The bug is not already filed publicly. Use available tooling:
               GitHub issues/PRs, GHSA security advisories, the project's
               SECURITY.md, and any disclosure pages the README points to.
            3. The bug is not already documented as a known limitation in code
               comments, TODO/FIXME annotations, or known-bug lists at or near
               the cited locations.

            Reject criteria — `rejection_class: duplicate-claim` for all of these:
            - Already fixed in HEAD or a recent commit: cite the fixing commit
              hash in `summary`.
            - Already filed as an issue/PR/advisory: cite the URL in `summary`.
            - Documented as a known/accepted limitation: explain in
              `follow_up_action` that maintainers are already aware.

            OUT OF SCOPE for THIS verifier:
            - Whether the PoC works (verifier 1 handled it).
            - Whether the bug is in scope (verifier 2 handled it).

            Use repo-local tools aggressively: `git log -S '<symbol>' --all`,
            `git log --grep='<keyword>'`, `gh issue list`, `gh pr list`,
            `rg -i '<keyword>'` against CHANGELOG and SECURITY.md. If you cannot
            determine novelty within reason, lean toward PASS — false positives
            on novelty are recoverable; false negatives waste submission budget.
      reporter:
        backend: claude
        prompt: |
          ## Specialized scope: per-bug report writer

          A claim has just passed every verifier in the chain (poc,
          root-cause-dedup, scope, novelty). Your job is to write a durable,
          human-readable per-bug
          report so the finding is preserved on disk independent of any
          end-of-run summarization. The runner verifies success by checking
          that `report.md` exists in the report directory; if it does not,
          you will be retried.

          Do NOT modify project source code. Treat the repository as
          read-only except for the report directory the runner gives you.

          The report should be brief but complete:
          - **Title**: one-line summary, suitable for a bug-bounty submission.
          - **Severity** (critical / high / medium / low) with a one-sentence
            justification grounded in the impact.
          - **Primary location** (`path:line` of the sink) and any secondary
            locations (caller chain or related sites).
          - **Description**: what the bug is and why it matters. Two or three
            short paragraphs at most.
          - **Proof of Concept**: the exact reproduction steps, script, or
            input. **If the claim packet contains a sanitizer crash report
            (ASan/UBSan/MSan) or a SIGSEGV/SIGABRT trace, paste it verbatim
            here, including the SUMMARY line and stack frames** — that
            output is the highest-value piece of evidence in the file.
          - **Impact**: what an attacker can achieve, in concrete terms
            (RCE? info disclosure? DoS only?).
          - **Verifier consensus**: a one-line note stating which verifiers
            passed (e.g., "poc PASS, scope PASS, novelty PASS"). Pull this
            from the verifier-consensus block in your prompt.

          Place any concrete PoC artifacts (input files, scripts, payloads)
          alongside `report.md` in the report directory. At minimum, write a
          `poc` file that captures the trigger so a maintainer can reproduce
          without rebuilding context. Use a sensible extension if applicable
          (`poc.py`, `poc.sh`, `poc.bin`, etc.).

          Overwriting an existing report at this path is fine — this step
          is idempotent.

  - id: summarize-findings
    prompt: |
      Write `bug-bounty-report.md`, a top-level index of the run's findings.

      Target repository: {{ REPO }}

      ## Primary input: per-bug reports under `output/`

      The reporter agent has already written one directory per verified claim
      under `output/<bug-id>/`, each containing a `report.md` plus PoC
      artifacts. Treat those as the authoritative source for verified findings:

      1. List `output/*/report.md` (e.g., `ls output/*/report.md` or
         `find output -maxdepth 2 -name report.md`).
      2. Read each one and use its content directly. Do NOT rewrite the
         per-bug report bodies — link to them and quote the title/severity/
         location into the index.

      ## Secondary input: state JSON

      Read `.juvenal-state-hunt-bugs-analysis.json` for context the per-bug
      reports do not capture: rejected claims (with verifier reasons),
      blocked targets, no-findings targets, captain coverage notes.
      Cross-check that every claim with `status: "verified"` in the state
      file has a matching `output/<claim-id>/report.md`. If any are missing,
      flag them in the report under "Reporter gaps" so the user knows the
      reporter step did not complete for that claim.

      ## Output format

      ## Verified Findings

      For each verified finding, emit a compact entry in this index:
      - **[Title from report.md](output/<bug-id>/report.md)** — Severity, Location.
        One-sentence summary of impact.

      ## Rejected Claims

      Briefly list claims that were rejected and why (e.g., guard found,
      precondition not met). This helps avoid duplicate investigation in
      future runs. Pull these from the state JSON.

      ## Incomplete Targets

      List any targets that were blocked, exhausted, or returned no findings,
      with brief notes on what was tried and why investigation stalled.

      ## Reporter gaps (if any)

      List any verified claims in the state JSON that do NOT have a
      corresponding `output/<bug-id>/report.md`. The user can re-run with
      `--resume` to re-trigger the reporter step.

      ## Recommendations

      - Suggested next investigation areas based on open questions
      - Subsystems that were not fully explored
      - Any patterns observed across multiple findings

      If the run produced no verified findings, state that clearly and explain
      what was investigated.
